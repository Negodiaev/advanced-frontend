import { TMods, classNames } from './classNames/classNames';
import { noop } from './noop/noop';
import { renderComponent } from './tests/renderComponent/renderComponent';

export {
  TMods, classNames, noop, renderComponent,
};
